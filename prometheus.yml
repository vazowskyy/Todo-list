global:
  scrape_interval: 10s
scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "flaskapp"
    static_configs:
      - targets: ["app:80"]
    metrics_path: "/metrics"
